<%= form_with(model: schedule, local: true) do |form| %>
  <% if schedule.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(schedule.errors.count, "error") %> prohibited this schedule from being saved:</h2>

      <ul>
      <% schedule.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :event_id %>
    <%= form.text_field :event_id %>
  </div>

  <div class="field">
    <%= form.label :week_day %>
    <%= form.number_field :week_day %>
  </div>

  <div class="field">
    <%= form.label :current_date %>
    <%= form.date_select :current_date %>
  </div>

  <div class="field">
    <%= form.label :hour %>
    <%= form.number_field :hour %>
  </div>

  <div class="field">
    <%= form.label :minute %>
    <%= form.number_field :minute %>
  </div>

  <div class="field">
    <%= form.label :coupons %>
    <%= form.number_field :coupons %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
